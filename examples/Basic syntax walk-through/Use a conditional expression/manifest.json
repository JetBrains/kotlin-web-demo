{
    "name": "Use a conditional expression",
    "files": [
        {
            "type":"SOURCE_FILE",
            "filename":"Basic syntax walk-through/Use a conditional expression/Use a conditional expression.kt"
        }
    ],
    "args": "10 20",
    "confType": "java",
    "help": "<a href=\"http://confluence.jetbrains.net/display/Kotlin/Control+structures#Controlstructures-Ifexpression\"><b>if</b></a> is an expression, i.e. it returns a value. Therefore there is not ternary operator (condition ? then : else), because ordinary <b>if</b> works fine in this role."
}